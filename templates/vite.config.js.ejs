import { defineConfig } from 'vite';
<% if (usesTypescript) { %>import react from '@vitejs/plugin-react';
    import tsconfigPaths from 'vite-tsconfig-paths';<% } else { %>import react from '@vitejs/plugin-react';<% } %>

            export default defineConfig({
            plugins: [
            react()<% if (usesTypescript) { %>,
                tsconfigPaths()<% } %>
                    ]
                    });